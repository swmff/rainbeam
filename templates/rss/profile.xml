<?xml version="1.0"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
   <channel>
      <title>{{ other.username }}</title>
      <link>{{ config.host }}/@{{ other.username }}</link>

      {% if let Some(biography) = other.metadata.kv.get("sparkler:biography") %}
      <description>{{ biography }}</description>
      {% else %}
      <description>@{{ other.username }}'s feed</description>
      {% endif %}

      <language>en-us</language>
      <generator>Neospring</generator>
      <atom:link href="{{ config.host }}/@{{ other.username }}/rss" rel="self" type="application/rss+xml" />

      <ttl>60</ttl>

      {% for response in responses %}
          <item>
              <title>{{ response.0.author.username }}: "{{ response.0.content }}"</title>
              <description>{{ response.1.content }}</description>

              <link>{{ config.host }}/response/{{ response.1.id }}</link>
              <comments>{{ config.host }}/response/{{ response.1.id }}</comments>
              <guid>{{ response.1.id }}</guid>

              <source url="{{ config.host }}/@{{ other.username }}/rss">{{ other.username }}</source>
          </item>
      {% endfor %}
   </channel>
</rss>
